function countCharacters(str) {
    return str.length;
}

// Exemple d'utilisation :
const inputString = "Bonjour";
const characterCount = countCharacters(inputString);
console.log(characterCount);  // Sortie: 7
